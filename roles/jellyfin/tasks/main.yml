- name: Ensure Jellyfin directories exist with correct ownership
  ansible.builtin.file:
    path: "/Media"
    state: directory
    owner: "1000"
    group: "1000"
    mode: "0755"
  loop:
    - config
    - cache
