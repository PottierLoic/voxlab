users:
  {{ authelia_admin_username }}:
    displayname: "{{ authelia_admin_username | capitalize }}"
    password: "{{ authelia_admin_password_hash }}"
    email: "{{ authelia_admin_email }}"
    groups:
      - admins